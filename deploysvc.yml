apiVersion: v1
kind: Service
metadata:
    name:
    labels:
       app: myjavaapp

spec:
  type: Nodeport
  ports:
  - port: 8080
    nodeport: 30001
    protocol: TCP
  selector:
       app: myjavaapp
